security:
  basic:
    enabled: false
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format-sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect