
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banks_api_test
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLInnoDBDialect
#        format_sql: true

#    to change base url of the backend